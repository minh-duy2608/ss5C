#include <stdio.h>

int main(){
	int a,b,c,lonNhat;
	printf("Nhap 2 so nguyen duong: ");
	scanf("%d %d",a,b);
	while(b != 0){
		b=c;
		a%b == b;
		a=c;
			if(a%b==0){
		printf("Uoc chung lon nhat cua %d va %d la: %d ", a,b,lonNhat);

	}
}
    return 0;
}
